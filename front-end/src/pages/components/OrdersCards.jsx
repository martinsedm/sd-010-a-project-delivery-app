import React, { /* useContext */ } from 'react';
// import Provider from '../../store/Provider';
import OrderCard from './OrderCard';

function OrdersCards() {
//  const { orders } = useContext(Provider);

  return (
    <OrderCard />
  );
}

export default OrdersCards;
